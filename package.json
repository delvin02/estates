{
  "name": "estates-monorepo",
  "module": "index.ts",
  "type": "module",
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5.0.0"
  },
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "dev": "cd packages/app && bun run dev",
    "build": "cd packages/app && bun run build",
    "lint": "cd packages/app && bun run lint"
  }
}